export PATH="$HOME/.cargo/bin:$PATH"
export GITHUB_EMAIL="$(rbw get --folder shell GITHUB_EMAIL)"
export GITHUB_NAME="$(rbw get --folder shell GITHUB_NAME)"
export MEGALLM_API_KEY="$(rbw get --folder shell MEGALLM_API_KEY)"
export BW_SESSION="$(rbw unlock)"
export NGROK_AUTHTOKEN="$(rbw get --folder shell NGROK_AUTH_TOKEN)"
export CLOUDFLARE_TUNNEL_TOKEN="$(rbw get --folder shell CLOUDFLARE_TUNNEL_TOKEN)"
export PANGOLIN_TOKEN="$(rbw get --folder shell PANGOLIN_TOKEN)"
export SANITY_API_KEY="$(rbw get --folder shell SANITY_API_KEY)"
export BWS_ACCESS_TOKEN="$(rbw get --folder shell BWS_ACCESS_TOKEN)"
export ZROK2_ADMIN_TOKEN="$(rbw get --folder shell ZROK2_ADMIN_TOKEN)"
export CLOUDFLARE_API_TUNNEL="$(rbw get --folder shell CLOUDFLARE_API_TUNNEL)"
export ANTHROPIC_BASE_URL=https://agentrouter.org/
export ANTHROPIC_AUTH_TOKEN="$(rbw get --folder shell ANTHROPIC_AUTH_TOKEN)"
#export ATUIN_SYNC_ADDRESS=https://atuin.pratyay.qzz.io
export ANTHROPIC_SMALL_FAST_MODEL='deepseek-v4-flash'
export ANTHROPIC_MODEL='glm-5.3'
export FREEMODEL_DEV="$(rbw get --folder shell FREEMODEL_DEV)"
export ANYAPI_KEY="$(rbw get --folder shell ANYAPI_KEY)"
export NVIDIA_API="$(rbw get --folder shell NVIDIA_API)"
export MISTRAL_API="$(rbw get --folder shell MISTRAL_API)"
export BITBUCKET_PAT="$(rbw get --folder sourceforge BITBUCKET_PAT)"
export SOURCEHUT_PAT="$(rbw get --folder sourceforge SOURCEHUT_PAT)"
export CODEBERG_PAT="$(rbw get --folder sourceforge CODEBERG_PAT)"
export GITGUD_PAT="$(rbw get --folder sourceforge GITGUD_PAT)"
export GITHUB_PAT="$(rbw get --folder sourceforge GITHUB_PAT)"
export GITLAB_PAT="$(rbw get --folder sourceforge GITLAB_PAT)"
export OPENCODE_ZEN="$(rbw get --folder shell OPENCODE_ZEN)"
export XIAOMI_API="$(rbw get --folder shell XIAOMI_API)"
export GROQ_API="$(rbw get --folder shell GROQ_API)"
export CEREBRAS="$(rbw get --folder shell CEREBRAS)"
export ANT_LING="$(rbw get --folder shell ANT_LING)"
export GOOGLE_STUDIO="$(rbw get --folder shell GOOGLE_STUDIO)"
export LLM7_API="$(rbw get --folder shell LLM7_API)"
export SOURCE_HUT_PAT="$(rbw get --folder shell SOURCE_HUT_PAT)"
export GH_TOKEN="$(rbw get --folder shell GH_TOKEN)"
export TOKEN_ROUTER="$(rbw get --folder shell TOKEN_ROUTER)"
export DNSSIMPLE_API="$(rbw get --folder shell DNSSIMPLE_API)"
export CN_API_KEY="$(rbw get --folder shell CN_API_KEY)"
export NEOCITIES_USER="$(rbw get --folder shell NEOCITIES_USER)"
export NEOCITIES_PASS="$(rbw get --folder shell NEOCITIES_PASS)"
